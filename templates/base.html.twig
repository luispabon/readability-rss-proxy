<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"/>
    <title>Readability RSS Proxy{% block title %}{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style type="text/css">
        body {
            font-size : 16px;
        }

        .menu .item {
            font-size : 1.3em;
        }

        form label, form input{
            font-size: 1.3em !important;
        }

        button.ui {
            font-size : 1em !important;
        }

        .bodyblock.ui.container {
            margin : 3em 0;
            width  : 90%;
        }

        .ui.container h1 {
            font-size   : 2em;
            font-weight : normal;
        }

        .ui.container h2.dividing.header {
            font-size   : 2em;
            font-weight : normal;
            margin      : 4em 0em 3em;
        }


        .ui.table {
            table-layout : fixed;
        }

        button a {
            color : black;
        }

        .orange a {
            color : white;
        }

        .modal iframe {
            border: none;
            width: 100%;
        }

        #items_container .item {
            cursor: pointer;
            padding: 1em 0;
        }

        #items_container .item:hover {
            background-color: #ddd;
        }

        #article img,
        #article iframe {
            max-width: 100%;
            max-height: 10%;
            display: block;
            text-align: center;
        }

        #article figcaption {
            font-weight: bold;
            font-style: italic;
            text-align: center;
        }
    </style>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.js"></script>
    {% block stylesheets %}{% endblock %}
</head>
<body>
<div class="ui">
    {% if app.user and embedded == false %}
        <div class="ui attached inverted menu" id="menu">
            <a class="item {% block reader_section_active %}{% endblock %}" href="{{ path('reader_index') }}">
                <i class="book icon"></i> Reader
            </a>
            <a class="item {% block feed_section_active %}{% endblock %}" href="{{ path('feed_index') }}">
                <i class="rss icon"></i> Feeds
            </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="item {% block users_section_active %}{% endblock %}" href="{{ path('user_index') }}">
                    <i class="user icon"></i> Users
                </a>
            {% endif %}

            <div class="right menu">
                <a class="item" href="" onclick="alert('not yet implemented')">
                    <i class="user circle icon"></i>
                    {{ app.user.getEmail() }}
                </a>
                <a class="item" href="{{ path('app_logout') }}">
                    <i class="logout icon"></i>
                    Logout
                </a>
            </div>
        </div>
    {% endif %}
    <div class="bodyblock ui container">
        <div class="column">{% block body %}{% endblock %}</div>
    </div>
</div>

{% block javascripts %}{% endblock %}
<script src="/js/semantic-tablesort.js"></script>
</body>
</html>
