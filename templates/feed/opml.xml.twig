<opml version="2.0">
    <body>
    <outline text="Subscriptions" title="Subscriptions">
        {% for feed in feeds %}
        <outline xmlUrl='{{ feed.getFeedUrl() }}' />
        {% endfor %}
    </outline>
    </body>
</opml>
