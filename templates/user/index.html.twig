{% extends 'user/users_base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="ui table">
        <thead>
        <tr>
            <th>Email</th>
            <th>Roles</th>
            <th style="width: 18em">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.email }}</td>
                <td>
                    <ul>
                        {% for role in user.roles %}
                            <li>{{ role }}</li>
                        {% endfor %}
                    </ul>
                </td>
                <td style="width: 5em">
                    <a href="{{ path('user_edit', {'id': user.id}) }}">
                        <button class="ui right labeled icon button">
                            <i class="edit icon"></i>
                            Edit
                        </button>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_new') }}">
        <button class="ui right labeled icon primary button">
            <i class="plus icon"></i>
            Add new user
        </button>
    </a>
{% endblock %}
